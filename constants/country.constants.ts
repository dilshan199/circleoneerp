// Country status constants
export enum CountryStatus {
    ACTIVE = "ACTIVE",
    INACTIVE = "INACTIVE",
    DELETED = "DELETED"
}

export const COUNTRY_STATUS_OPTIONS = Object.values(CountryStatus);